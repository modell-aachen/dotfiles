pseudo-install() {
    c_d=$PWD;
    cd /var/www/qwikis/qwiki;
    sudo -u www-data ./pseudo-install.pl -u "file://${REPOS}/$1";
    sudo -u www-data ./pseudo-install.pl -e "fiel://${REPOS}/$1";
    cd $c_d;
}

