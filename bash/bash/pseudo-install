pseudo-install() {
    c_d=$PWD;
    cd /var/www/qwikis/qwiki;
    for repo in "$@"
    do
        sudo "HOME=$HOME" -u www-data ./pseudo-install.pl -u "$repo";
        sudo "HOME=$HOME" -u www-data ./pseudo-install.pl -e "$repo";
    done
    cd $c_d;
    sudo systemctl restart apacheRestart;
}

