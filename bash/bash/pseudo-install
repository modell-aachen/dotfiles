pseudo-install() {
    c_d=$PWD;
    cd /var/www/qwikis/qwiki;
    for repo in "$@"
    do
        sudo -u www-data ./pseudo-install.pl -u "$repo";
        sudo -u www-data ./pseudo-install.pl -e "$repo";
    done
    cd $c_d;
}

